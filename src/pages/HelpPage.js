import React from 'react';
import MapView from "../components/MapView"

function HelpPage () {
    return (
      <div className="screen-container">
        <p>Click the Scan button to begin using the app!</p>
      </div>
    );
}

export default HelpPage;